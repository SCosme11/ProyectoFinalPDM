import 'package:flutter/material.dart';

class PantallaLogsSensores extends StatelessWidget {
  const PantallaLogsSensores({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Logs de Sensores')),
      body: const Center(child: Text('Aquí irá el diseño responsivo (Punto 6)')),
    );
  }
}